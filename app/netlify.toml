[build]
    functions = "functions"
    publish = "dist/"
    command = "npm install && npm run build"
    
    [[redirects]]
  from = "/register"
  to = "index.html"
  status = 200
  force = true
  headers = {X-From = "Netlify"}
    
   
